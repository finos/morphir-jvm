package morphir.flowz

import zio.test._

object StepSpec extends DefaultRunnableSpec {
  def spec = suite("Step Spec")(
    //   testM("It should be possible to create a Step that always succeeds with a value")(

    //   )
  )
}
